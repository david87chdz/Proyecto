{% extends 'base.html.twig' %}

{% block title %}TipoMesa{% endblock %}

{% block body %}
    <h1>TipoMesa</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipo_mesa.id }}</td>
            </tr>
            <tr>
                <th>Ancho</th>
                <td>{{ tipo_mesa.ancho }}</td>
            </tr>
            <tr>
                <th>Largo</th>
                <td>{{ tipo_mesa.largo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tipo_mesa_index') }}">back to list</a>

    <a href="{{ path('app_tipo_mesa_edit', {'id': tipo_mesa.id}) }}">edit</a>

    {{ include('tipo_mesa/_delete_form.html.twig') }}
{% endblock %}
